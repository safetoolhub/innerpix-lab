Al cambiar el nivel de log en la configuraicon debe cambiarse en todos los loggers de lso ficheros auxiliares, porque tienen su propia configuracion
Probar todo y  meterle tests unitarios
Mejorar el interfaz de las pestañas y el about
Comprobar backups
Comprobar que funciona bien con extensiones en mayusculas y minusculas
Probar todo con muchos archivos a ver ux y rendimiento
En la opcion de deteccion de fotods de whatsapp no me detecta las de test
Hay que pasar a dataclass toda la gestion de results dentre stages y dialogs...en lugar de dicts
CProbar como reacciona stage 2 ante errores, por ejemplo poner un label de icono que no exista o conseguir error de alguna forma
Con dry-run es posible que se haga reanalisis tambien

Proximos pasos de refactor:
Integración en Diálogos - Refactorizar diálogos para usar View Models (opcional)
Tests Unitarios - Crear tests/test_view_models.py (opcional)
CLI Tools - Scripts de exportación usando View Models (opcional)
Documentación - Tutorial de uso de View Models (opcional)

Verificar que los directorios que se ponen para logs y backups existan

Lo smensajes de la card en stage 3 de archivos similares son un desastre al volver del analisis. Hay que mejorarlos. 
En general mejorar los mensajes de todas las cards. 
Lo de los logs con bloqueo igual no hace falta porque el problema de desorden era otro
Hay que poner avisos al usuario de directorios muy largos antes del analisis
el tema del mumero de workers paralelos está raro. se puede configurar desde settings pero tambien se hace en el config a pelo, hay que organizarlo bien. 
En heic quedan detalles esteticos por pulir en las tablas

Pruebas con ficheros con distintas fechas
1 mes: exiftool -AllDates+="0:1:0 0:0:0" IMG_REFERENCIA_HORADIFERENTE_POCA.jpg
2 segundos: exiftool -AllDates+="0:0:0 0:0:2" IMG_LIVEPHOTO_HORADIFERENTE_2segundos.JPG

funcionaria el caluclo de memoria y de threads dinamico en algo que no sea linux?

similar files
The clustering algorithm is O(N^2) and naturally slow for 20k+ files. Future work could involve optimizing this algorithm (e.g., using BK-trees or other indexing methods) or offloading it to a background thread more effectively.

Revisar que el cálculo de similitud es incorrecto cuando la distancia Hamming promedio (avg_hamming) es mayor que MAX_HAMMING_THRESHOLD (20). No s eentiende bien los grupos que salen. 


FUTURO
---------
v 2.0 poder elegir una foto de live photos del video
